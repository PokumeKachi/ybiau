# Y(our) B(rain) I(n) A(SCII)/U(TF-8)

> Can you imagine storing the entire knowledge of yours in just pure text...?

- The structure is just notes in a flat directory (notes/), with the exception of draft notes.
